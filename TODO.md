# TODO

- [ ] git-autocommit and git-commit-dated by default does not commit files whose diff only includes deletion of 2 or more empty lines
- [ ] command `git fixtimes` essentially runs `path fix times` on every file in order to set the mtime to ctime
